namespace Minecraft {
    public enum Direction {
        Right,   // +x direction
        Left,    // -x direction
        Down,    // -y direction
        Up,      // +y direction
        Forward, // +z direction
        Back     // -z direction
    }
}
